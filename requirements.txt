# Canonical dependency file for Streamlit Cloud. Community Cloud installs from this file only.
# Do not rely on pyproject.toml for Cloud; keep anthropic here for Claude (Intelligence Desk).
# Streamlit Cloud / production runtime (app only; ETL has its own env if needed)
streamlit>=1.28
pandas>=2.0
pydantic>=2
pyyaml
plotly
duckdb
tabulate>=0.9.0
reportlab
matplotlib>=3.5
seaborn>=0.12
anthropic>=0.39
openai>=1.0
